<!doctype html>
<html>
 <head>
  <title>WebCampBackendNew</title>
<meta name="csrf-param" content="authenticity_token">
<meta name="csrf-token" content="DRDHX/UXcQgrFT0gCt8nsLVOnSovSfr4kjgnA0C1ShZNzHRu8ArP7V3LUVRYqmK+FTF6vMViYlQy5UNlqgY39Q==">
</head>

</body>
<p id="notice"></p>
<h1>Books</h1>


  <table>
    <thead>
  		<tr>
  			<th>Title</th>
  		    <th>Body</th>
  		    <th colspan="3"></th>
  		</tr>
    </thead>
 	<tbody>
<% @posts.each do |post| %>

  		<tr>
  		    <td><%=  post.title%></td>
  			<td><%= post.body %></td>
  			<td><%= link_to "Show", post_path(post.id),class: "show_#{post.id}" %></td>
        <td><%= link_to "Edit", edit_post_path(post.id),class: "edit_#{post.id}" %></td>
  			<td><%= link_to "Destroy", destroy_post_path(post.id),class: "destroy_#{post.id}", method: :delete, "data-confirm" => "Are you sure?" %>
        </td>
  		</tr>
<% end %>
  	</tbody>
  </table>
  <h2>New book</h2>
<%= form_for(@post) do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title,class: "book_title" %>

  <h4>Body</h4>
  <%= f.text_area :body,class: "book_body" %>

  <br><%= f.submit 'Create Book' %>

<% end %>

  </div>
</form>
</body>
</html>
